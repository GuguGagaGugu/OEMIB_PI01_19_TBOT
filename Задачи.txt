1) В siem системе фигурируют события с ip-адресом
Сервис должен позволяет отправку ip адресов или url или hash с помощью a
На c#, на последней версии, запускаться в контейнере, писать через openapi, должна быть логика data transfer и т.д 

2) Написать парсер для отчета и сохранял информацию в базу данных. 
Служба windows мониторит одну папку как только появился файл mht запускает парсер и вводит в бд, на .net, должен срабатывать callback, (должен сохранять(информация об отчете(одна из таблиц), n# отчета, даты, имя, профиль; Узлы(ОС, IP и ) надо написать в ноды;Сервисы(Открытые порты, имя приложения и все что известно, ссылки) ))
Из 